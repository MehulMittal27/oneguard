# backend

FastAPI service: deterministic decision engine, policy compiler, Viseca sandbox client and
long-poll worker, and the `/api` implemented against `docs/api-contract.md`.

See `docs/architecture.md` for the module layout and `CLAUDE.md` for the rules that bind
every change here. Start with `make replay SCEN=SCEN0001` after `make setup`.
