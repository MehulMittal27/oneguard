# Decisions log

Choices made where the spec, the data, or the platform disagreed. Newest first.

| Date | Decision | Why | Where |
|---|---|---|---|
| 2026-09-24 | A step-up the platform serves again from `/v1/decision-requests/next` (envelope `status: "pending_step_up"`) is not re-posted; the worker makes sure its expiry is scheduled and pauses 0.5 s before the next poll | Live smoke (SCEN0000): the platform serves a waiting step-up on every poll with no long-poll wait, and a second decision is 409 `step_up_resolution_required`; the worker spun at ~24 polls + 24 re-posts per second for the whole 120 s window | viseca/worker.py `_step_up_still_waiting`; tests/fake_viseca.py |
| 2026-09-24 | A step-up's deadline is the decision reply's `step_up_expires_at`; a 409 `authorization_not_pending` / `authorization_finalized` on the timeout `/resolve` is expected, not an error | Live smoke: the platform expires the step-up itself at that moment (decline, `decision_source: timeout`, "The confirmation window expired.") and got there 26 ms after the deadline, before our resolve; our ledger outcome (declined, expired, not spent) is the same. Rules Q2's message therefore rarely reaches Viseca | viseca/worker.py `expire`, `_await_answer`; api-contract.md §3.5 |
| 2026-09-24 | The worker downloads the history file at start and hashes it | Live `/v1/reference-data` names the file (`history: {path, rows, format}`) but serves no SHA-256; on 24 Sep the served file was byte-identical to `data/metadata.json` (eb562bdc…) | viseca/worker.py `_check_reference_data` |
| 2026-09-24 | Timeouts come from bootstrap `limits.decision_timeout_seconds` (8), `limits.step_up_timeout_seconds` (120), `limits.long_poll_max_seconds` (25) | Live bootstrap shape; the human window had matched only the default before | viseca/worker.py `_load_settings` |
| 2026-09-24 | `POST /v1/team/reset` sends `{"confirmed": true}` | Live: no body → 422 `validation_error` (`details` names `body.confirmed`); with it → 403 `reset_disabled` "Team reset is disabled during judging" (bootstrap `features.reset: false`), so the team's sandbox state cannot be cleared | viseca/client.py `reset_team` |
| 2026-09-24 | "M1" Session watch after a burst: the next otherwise-clean purchase asks once (`session_watch`); the customer's approval turns the watch off, a no or a timeout keeps it on. Per card, carries into later live sessions; replays keep their own | P2 bundle v2 (PM decision). rules.md W-rule 4 contradicted api-contract §3.6; the contract's reading wins | rules.md W-rule 4; acceptance-oracle.yaml AU0031, AU0032; api-contract.md §3.6, §4; engine/ledger.py `_frozen`, engine/decide.py |
| 2026-09-24 | "D8" SCEN0001, branch "AU0006 pending (reserved)": AU0009 → `step_up` (C2, M5): 258.50 on final approvals fits, 389.00 only with AU0006 and AU0008 reserved. Knock-on: AU0011 → `step_up` (C2, M5): 158.00 fits, 312.50 only with three orders waiting | P2 bundle v2. rules.md M5: a breach caused only by reservations asks | acceptance-oracle.yaml AU0009, AU0011 |
| 2026-09-24 | "M13(a)" "Two tickets" = total quantity of the requested item across the cart (1 line × 2 or 2 lines × 1), field `cart.quantity` | P2 bundle v2 (PM decision). Per-line `items[].quantity = 2` rejects legitimate 2 × 1 baskets | acceptance-oracle.yaml unseen_instructions; api-contract.md §3.3 `cart.quantity`; engine/policy.py |
| 2026-09-24 | "M13(b)" "Arrive by Friday" = the Friday after the instruction was confirmed, as a literal date; "by" = on or before | P2 bundle v2 (PM decision). A per-purchase Friday would let later purchases arrive later | acceptance-oracle.yaml unseen_instructions |
| 2026-09-24 | "M13(c)" "Same price as last time, ask me if anything changed": a different price asks (`on_fail: ask`), it is not declined; "renew" = the same gym, also `on_fail: ask` | P2 bundle v2 (PM decision). The customer's words say ask | acceptance-oracle.yaml unseen_instructions; engine/types.py `Rule.on_fail` |
| 2026-09-24 | "M13(d)" "The present I picked" / "official ticket seller" are rules with field `unverifiable`: always unknown, ask once, then remember the customer's yes for that shop and item under that mandate | P2 bundle v2 (PM decision). No data can check them (T2); remembering cuts repeat approvals | acceptance-oracle.yaml unseen_instructions; api-contract.md §3.3 `unverifiable`, §2 `MandateUsage.confirmations`; engine/ledger.py `_confirmed_keys`, engine/policy.py `add_ledger_results` |
| 2026-09-24 | "M18" Every run writes `runs.kind` (`live` / `replay`); remembered answers and the session watch carry over only between live runs, and a run with no `runs` row counts as a replay. The worker writes its live row before a run's first decision | P2 bundle v2. Replaying a scenario must always decide the same way | viseca/worker.py `_handle`; database.md `runs` |
| 2026-09-24 | "M20" Reason codes `rule_not_met`, `unusual_activity`, `session_watch` | P2 bundle v2. decide emits them; every code is listed before it is emitted | api-contract.md §4 |
| 2026-09-24 | "M10" `backend/scripts/bench_engine.py` belongs to P2 | P2 bundle v2. The plan asks P2 to write the engine benchmark (P2-3) | team-contract.md §1 |
| 2026-09-24 | No returns (0 days) for: final sale, all sales final, no returns, no refunds, non-returnable, non-refundable (except when the clause is about shipping, delivery or a fee), returns not accepted, cannot be returned, not eligible for return | P2 bundle v2 (product decision) | engine/facts.py `return_window_days`; rules.md C7 |
| 2026-09-24 | Customer rules are checked only when the customer stated them (no known-shop check unless asked) | P2 bundle v2 (product decision) | engine/policy.py `evaluate_rules` |
| 2026-09-24 | Size rules apply to the requested item's lines only; add-ons are judged by "nothing extra" (C10) and the recurring-cost protection (A6) | P2 bundle v2 (product decision) | engine/policy.py; rules.md C6 |
| 2026-09-24 | A restriction we have no data for ("official ticket seller") is unknown, never a pass | P2 bundle v2 (product decision) | engine/policy.py; rules.md T2 |
| 2026-09-24 | Period limits count final approvals plus reserved pending step-ups; a breach caused only by reservations asks (`period_reserved_pending`, M5) | P2 bundle v2 (product decision) | engine/policy.py `evaluate_period_rule`; engine/decide.py |
| 2026-09-24 | engine overrun follows the uncertainty setting (rules D2, D3) | Captain decision. An engine still unfinished `POST_MARGIN_S` before `deadline_at` follows `policy.uncertainty_policy` of the policy the worker decides with (the bound policy, else the event's mandate snapshot): `ask` → `step_up`, `decline` → `decline`, `approve` → `step_up`, all with `unevaluable`. Approve is never automatic (D2). The step-up is exactly the one of the row below (message, reservation, C8, timeout expiry). The decline says "Declined: we could not finish checking this purchase in time, so nothing was approved.", is final, reserves and counts nothing, and schedules no expiry. A missing or unknown setting is read as `ask` (logged). Either way the late engine result records the claimed decision (first claim wins) and posts nothing. Engine errors and schema-invalid events still decline | viseca/worker.py `_overrun_entry`, `_post_overrun`, `overrun_setting`, `OverrunClaims` |
| 2026-09-24 | Superseded by "engine overrun follows the uncertainty setting" above. engine overrun → step_up (rules D3, P6) | Captain decision. D3: what is known may be Ask; P6: a slow check must not block ordinary shopping, and nothing is approved without the customer. An engine still unfinished `POST_MARGIN_S` before `deadline_at` posts `step_up`, `unevaluable`, "We couldn't check this purchase in time; please review it", then waits on the customer like any step-up: pending ledger entry with its reservation, C8, expiry at accepted time + human window with the timeout decline. The late engine result records that step-up (first claim wins) and posts nothing. Engine errors and schema-invalid events still decline. Supersedes the overrun part of the "Engine over budget …" row below | viseca/worker.py `_overrun_step_up`, `OverrunClaims` |
| 2026-09-24 | Half EU sizes (43.5, 43,5, 43½, 43 1/2) and letter sizes (XS–XXXL, small/medium/large) are read; size ranges ("sizes S-XL") are not | P2 product decision (facts + policy PR) | engine/facts.py; api-contract.md §3.3 `items[].size_eu`, `items[].size_letter` |
| 2026-09-24 | "Non-refundable" means no returns, except when the clause is about shipping, delivery or a fee; "exchange / store credit only" is unknown | P2 product decision (facts + policy PR) | engine/facts.py `return_window_days`; rules.md C7 |
| 2026-09-24 | Recurring is billing language only; bare "weekly"/"monthly" in product names (8 grocery lines say "Weekly") is ignored | P2 product decision (facts + policy PR) | engine/facts.py `recurring`; rules.md A6 |
| 2026-09-24 | Shop-text extraction is English only for now | P2 product decision (facts + policy PR) | engine/facts.py |
| 2026-09-24 | Contradictory shop terms (two different values) are unknown and flagged `contradictory`, reason code `shop_terms_contradictory` | P2 product decision (facts + policy PR) | engine/facts.py; api-contract.md §4 |
| 2026-09-24 | Add-on lines without terms do not make the order's return window unknown; the order uses the strictest stated line | P2 product decision (facts + policy PR) | engine/facts.py; rules.md C7 |
| 2026-09-24 | Timeout message formats the `/v1/bootstrap` human window: "No answer within {n} s; nothing was approved"; identical to the documented text at the default 120 s | the message names the real number (E2) if the platform window ever differs | viseca/worker.py `timeout_message`; rules.md Q2 |
| 2026-09-24 | Viseca response shapes not fixed by the docs are read tolerantly: timeouts from the first bootstrap key naming human+window (seconds, `_ms` converted), history metadata = the reference-data object with `sha256` naming the history file, run progress `status`/`total`, step-up accepted time from `accepted_at`/`decided_at`/… else our receipt time, feed items `authorization_id` + `status` | no live key during P1-1; one place to adjust once the real payloads are seen | viseca/worker.py |
| 2026-09-24 | Reconciliation: `context.approved_spend_in_period_chf` vs our final approvals in the run over the policy's shortest period (whole run without one); feed mismatch only when Viseca reports a final status our ledger does not have. Both are `ledger_mismatch` info evidence (feed rows on the next decision) and never change an outcome | the platform's period is unspecified; transient pending states are not mismatches | viseca/worker.py; api-contract.md §3.4 |
| 2026-09-24 | Overrun part superseded by "engine overrun follows the uncertainty setting" above. Engine over budget and still unfinished `POST_MARGIN_S` before `deadline_at`, engine error, or an event failing the schema → `decline` with `unevaluable` before the deadline | D3 plus P3: what could not be checked is never approved; a step-up would ask the customer to approve an unchecked purchase | viseca/worker.py |
| 2026-09-24 | Our own mandate not active (revoked) → pipeline declines with `card_or_authority_inactive` (step 1) before rules run, stubs included | T6/Q6 must hold before P2's decide lands | pipeline.py |
| 2026-09-24 | Redelivery re-posts the stored decision; a 409 means Viseca has it. A 400/422 on the full decision body is retried once without `evidence`/`engine_version` | a decision must land before the deadline even if the platform rejects our evidence format | viseca/worker.py |
| 2026-09-24 | Customer answer: POST `/resolve` first, then the ledger (a failed POST changes nothing). Expiry: ledger first, then POST (the window is closed locally whatever Viseca says). Both serialised by one lock. `/resolve` evidence carries `{rule: resolved_by, outcome: info, detail: customer\|timeout}` | exactly one of them closes a step-up; nothing is approved after the window | viseca/worker.py |
| 2026-09-24 | A mandate with no confirmed policy bound in the worker is decided from the event's mandate snapshot (`hard_rules` as typed rules, convenience fields restated where the field vocabulary allows) | the snapshot is what the customer confirmed at Viseca | viseca/worker.py `policy_from_snapshot` |
| 2026-09-24 | team docs reconciled with core docs; database stack, timeout resolve, models path fixed | conflict list from the team-docs commit | CLAUDE.md / AGENTS.md rule 6, Stack; architecture.md Dependencies, Deployment; api-contract.md header, §3.1, §3.4, §3.5, §4, §6; database.md §1; team-contract.md §3.1; team-plan.md P1-0, P5-2; README.md Quick start |
| 2026-09-24 | Use every provided field and endpoint: step 1 names `authority_status` and `card_status_at_attempt`; C7 covers `order_cancellable`; `merchant_mcc` secondary evidence for C8; `merchants.recurring_capable` evidence for A6; W6 price outside catalogue range (weak); `HistoryIndex.agent_history` / `item_price_range`; `DryRunResult.agent_history`; reconciliation via `GET /v1/events` as well as `context`; startup pack check against `/v1/reference-data` | coverage audit of the data pack and API | rules.md §4 step 1, C7, C8, A6, W6; api-contract.md §2, §3.4; database.md §3; team-plan.md P1-1, P3-1, P5-2 |
| 2026-09-24 | Frontend contract merged into docs/api-contract.md; frontend/API-CONTRACT.md is a pointer | One canonical contract; the backend-facing requirements (envelopes, consistency matrix, check wording, checklist) now live in it | api-contract.md §1.0, §3.1a, §3.9, Appendix A; frontend/API-CONTRACT.md |
| 2026-09-24 | Spend meter reads `Mandate.usage` first, check wording second | `usage` is the engine ledger's own view; parsing limits back out of prose fails silently (the fixture's "across any rolling 7 days" never matched) | frontend/src/lib/spend.ts `limitsFromMandate`; api-contract.md §3.9 |
| 2026-09-24 | `Decision.explanation_source` ('template' \| 'model'); C6 may return the template message first and the model rewrite on a later poll | Viseca Q&A | api-contract.md §2, §3.4, §6 |
| 2026-09-24 | C8 no longer bound to a hash of the step-up rendering; the step-up still renders the complete purchase | Viseca Q&A | api-contract.md §3.5 |
| 2026-09-24 | Expired step-up: backend posts `/resolve` `decline` with the timeout message; `Decision.resolved_by: 'customer' \| 'timeout'` (NEW) | Viseca Q&A | api-contract.md §3.5 |
| 2026-09-24 | Tier 2 extraction and Tier 3 explanation use the compiler's provider interface (OpenAI first, model-agnostic); soft signals unchanged | Viseca Q&A | api-contract.md §3.7 |
| 2026-09-24 | P8: with every model off, each decision is identical or more cautious; models never change a decision the deterministic rules have made | Viseca Q&A | rules.md P8 |
| 2026-09-24 | Three tiers: 1 deterministic rules; 2 constrained LLM fact extraction only when a rule is `unknown` (schema-validated, 1.5 s, `source: model`); 3 LLM explanation rewrite after posting, template first | Viseca Q&A | rules.md §4a |
| 2026-09-24 | M5: a purchase that fails C2 only because of reservations is Ask, and the message names the waiting order | Viseca Q&A | rules.md M5 |
| 2026-09-24 | Q2 closed: expiry posts `/resolve` `decline` ("No answer within 120 s; nothing was approved", `resolved_by: timeout`); not spent | Viseca Q&A | rules.md Q2 |
| 2026-09-24 | Q6 closed: decline anything received after revoke; UI shows cancelled once the platform confirms | Viseca Q&A | rules.md Q6 |
| 2026-09-24 | Q7 closed: known shop is customer-level; Viseca handles it | Viseca Q&A | rules.md Q7 |
| 2026-09-24 | Q8 closed: superseded by the new M5 | Viseca Q&A | rules.md Q8 |
| 2026-09-24 | Q11: hidden scenarios exist at judging; C12 in scope, tested with invented instructions | Viseca Q&A | rules.md Q11 |
| 2026-09-24 | E8: after posting, an LLM may rewrite the explanation from structured evidence only; it never sees or alters the decision | Viseca Q&A | rules.md E8 |
| 2026-09-24 | AU0008 with AU0006 pending (reserved) → step_up [C2, M5] | Viseca Q&A | acceptance-oracle.yaml AU0008 |
| 2026-09-24 | Four invented instructions with expected typed rules; a phrase with no field stays `unknown` and becomes an open question | Viseca Q&A | acceptance-oracle.yaml `unseen_instructions` |
| 2026-09-24 | New typed-rule fields for C12: `items[].unit_price_chf`, `items[].quantity`, `merchant.merchant_country`, `authorization.delivery_by`, `authorization.weekday`, `authorization.local_hour` (Europe/Zurich) | Viseca Q&A | api-contract.md §3.3 |
| 2026-09-24 | `oneguard/llm/` provider interface (OpenAI first, Anthropic stub) shared by compiler, tier-2 extraction and tier-3 explanation | Viseca Q&A | architecture.md |
| 2026-09-24 | Dependencies: `openai` added, `anthropic` kept optional | Viseca Q&A | architecture.md |
| 2026-09-24 | Latency: tier 2 ≤ 1.5 s inside the 2 s budget; tier 3 after posting, outside it | Viseca Q&A | architecture.md |
| 2026-09-24 | CLAUDE.md rule 1: no model decides; models may extract a fact, rewrite the explanation after posting, or answer agent_directed via Laya (mirrored to AGENTS.md) | Viseca Q&A | CLAUDE.md, AGENTS.md |
| 2026-09-24 | `openai` added to the `compiler` extra alongside `anthropic` | Viseca Q&A | backend/pyproject.toml |
| 2026-09-24 | `ONEGUARD_LLM_PROVIDER=openai` and `OPENAI_API_KEY`; `ANTHROPIC_API_KEY` kept as the alternative; `ONEGUARD_COMPILER` removed; VITE_ lines removed (frontend has its own .env.example) | Viseca Q&A | .env.example |
| 2026-09-24 | A6 (hidden recurring cost) is Ask unless C10 is stated | Recurring cost is the risk; a customer may still want the plan; Ask is the useful intervention | rules.md §7, Q10 |
| 2026-09-24 | A1 flags the purchase, not the shop for the run | Otherwise AU0042/AU0045 could never approve at PixelHarbor | rules.md §7 |
| 2026-09-24 | W4 suppressed within a stated per-order limit | Fires on 7/11 SCEN0004 purchases otherwise | rules.md §8 |
| 2026-09-24 | Known shop = customer-level, pending Viseca answer | AU0044; issuer may only see own cards | rules.md Q7 |
| 2026-09-24 | Each purchase judged on its own (no fulfilment counter) | Viseca notes call AU0023 compliant and AU0042 a legitimate re-quote | rules.md Q1 |
| 2026-09-24 | Laya used for one question: agent_directed (threshold 0.6, 500 ms timeout, loaded once at startup, called from a thread pool). recurring_billing and item_kind dropped. | Spike: injections 0.71/0.83, no false positive above 0.38, P95 175 ms for three questions, 48 ms for agent_directed alone (P50 28 ms); recurring 0.46 miss on the only example; 13/13 monitors misclassified | signals.py |
