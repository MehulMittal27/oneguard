# Decisions log

Choices made where the spec, the data, or the platform disagreed. Newest first.

| Date | Decision | Why | Where |
|---|---|---|---|
| 2026-09-25 | A step-up decided at step 4 (an unknown rule under "ask me") also carries `injection_suspected` in `reason_codes` when A1 or the agent-directed soft signal triggered with an ask outcome (rules.md D4). The code follows the unknowns' codes; `deciding_ids`, the step and the message are unchanged. Declines keep only their own codes, and an info-only A1 (earlier at the shop) adds nothing. No public row changes: oracle 45/45, matrix regenerated with no diff | Live SCEN0136 AU10391 and SCEN0124 AU10261 asked with the injection in the evidence and the message but not in the codes, so anything reading the codes (the phone's reason labels, the judges) missed that the text tried to steer the agent. `injection_suspected` is already in api-contract §4 and `frontend/src/lib/reasonCodes.ts` | engine/decide.py step 4; rules.md §4 D4 |
| 2026-09-25 | OneGuard Passport (docs/passport.md). Every active policy is a signed passport (Ed25519, canonical JSON), every decision a signed receipt with `would_approve_if`, and anyone can verify both (`POST /api/verify`). C2, C4, C5, C8 and device approve / remove are device-bound: accepted only when signed (ECDSA P-256, WebCrypto, non-extractable) by a device enrolled on the card; the card's first device enrols without approval (trust on first use), later ones wait for an enrolled one; the last one cannot be removed; recovery is the operator reset outside prod. Receipts and the backfill are issued by a background sweep that reads only the store (decision row + stored event + passport in force), so signing never sits on a decision's path to Viseca; C8 and the receipt endpoint sign at once. `permitted` is read from the decision's evidence rows (check text or the engine's label), so old and new decisions get the same receipt. The passport lists the policy flags that restrict (`requested_item`, `nothing_extra`, …) beside the typed checks, since `would_approve_if` names them. `make demo-live` signs C2 with a key of its own per server (`~/.config/oneguard/device-<host>.pem`, `ONEGUARD_DEVICE_KEY`), which a card with an enrolled device must approve first | The captain's Passport brief: the customer, not whoever reaches the API, holds the leash; decisions are auditable after the fact by the issuer, a merchant or an auditor. A receipt must never delay or break a decision, and old rows must verify too. The 45 oracle outcomes and messages are byte-identical (docs/replay-matrix.md unchanged) | passport/; api/routes_passport.py, auth.py, routes_customer.py (C2, C4, C5, C8), routes_dev.py (reset); engine/explain.py `with_bounds`; engine/types.py; pipeline.py; viseca/worker.py `posted_evidence`; viseca/demo.py; store/schema.py, db.py `add_missing_columns`; api-contract.md §1.3, §3.10; database.md §2, §4 |
| 2026-09-25 | A period limit with no per-order limit is also the per-order cap: "Total per month must stay under CHF 80" gives C1 `authorization.billing_amount_chf < 80` (same operator, scope purchase, source inferred, "Each payment under CHF 80 (from your monthly limit)") beside the monthly C2. Both compiler paths add it (`parser.period_cap`; the LLM path settles the model's per-order question with it, and the prompt tells the model it is added), lint's `no_amount_cap` passes with it, and C2 accepts it. The tightest period limit when there are several; nothing when a per-order limit is stated or derived (`stay_cap`) or items are priced or counted. As the per-order limit it also feeds A4 (split order) and suppresses W4 within it, as the nights cap does. SCEN0136 compiles with no open question on both paths; its recording is re-recorded (the model no longer asks); the other nine compile the same under the new prompt (two live runs each, SCEN0101's requested_item variance aside). Oracle 45/45 and the matrix unchanged (the public policies are hand-built) | One payment at or above the whole period's limit breaks it on its own, so asking the customer "what is the most one purchase may cost?" asked for nothing new and left SCEN0136 unconfirmable in C2 without an answer (captain) | compiler/parser.py `period_cap`, llm.py; rules.md §10 |
| 2026-09-25 | "I need new X", "new X", "replace my X" and "get me X" name one requested item too, like "the X I chose", "one X", "a X" and "a pair of X" (`names_one_item`, both compiler paths, which share it through `finalize`), so A8 applies. A count of two or more ("two new shirts", "get me three shirts", "replace my 4 tyres") and a plural with none of these cues ("order hiking boots", "buy the running shoes", "renew my membership") stay several. Oracle: SCEN0002 ("Replace my worn road-running shoes") AU0019 approve -> step_up [A8, C7, already_fulfilled], AU0023 approve -> step_up [A8, already_fulfilled] (AU0012 bought the pair); totals 10/20/11 -> 8/20/13 (q10: 10/21/10 -> 8/21/12); `tests/fixtures/policies/SCEN0002.yaml` gains `single_item: true`; the regenerated matrix changes only those two rows and the outcome line. The model prompt is unchanged, so no recording changes | Live SCEN0130 ("I need new hiking boots, size 42 ...") compiled `single_item: false`, so A8 never applied and the boots were approved 5 times (CHF 688.28). Replacing a pair of shoes asks for one pair, as the captain read it | compiler/draft.py `names_one_item`; rules.md A8, Q1; acceptance-oracle.yaml SCEN0002 |
| 2026-09-25 | Laya reads only the shop-text lines the keyword check did not flag that have at least 6 words (NFKC, whitespace-split; `signals.MIN_WORDS`), and the budget is 1500 ms again: Fly secret `ONEGUARD_SIGNAL_BUDGET_MS=1500` (the code default stays 500). Supersedes the 975 ms of the row below. The keywords go first and stay authoritative: a flagged line keeps its keyword verdict and is not sent; a model answer can still only add an ask. The signal's detail says how many item lines the model read ("The model read 1 of 2 item lines."), and the log gives the lines and ms per call. On the pack the model reads 36 of 56 lines (2 flagged, 18 under 6 words): 11 purchases need no model call, and purchases with two model calls drop from 11 to 2. Laptop (MPS), 10 reps, `soft_signals` per purchase: P50 39 -> 33 ms, P95 74 -> 51 ms; outcomes identical, oracle 45/45 with the model loaded, matrix unchanged | SCEN0135 (cloud, `performance-2x`, 975 ms) went over budget on nearly every decision (8 warnings), so every decision fell back to keywords and still paid ~1,150 ms P50: the budget bought nothing. On `performance-2x` one model call is P50 ~740 ms, P95 ~865 ms (benchmark.md §3), so a purchase with one line read fits 1500 ms; the two-line purchases that went past it are now rare. Set by firstmate as a Fly secret at deploy, not by this commit | engine/signals.py `_for_model`; architecture.md Deployment; benchmark.md §2, §3; tests/test_protections_signals.py |
| 2026-09-25 | Laya budget 975 ms: Fly secret `ONEGUARD_SIGNAL_BUDGET_MS=975`, Laya still on `performance-2x`. Supersedes the 1500 ms of the row below; past the budget the keyword answer stands (P8). Follow-up after the demo: run Laya only on the lines the keyword check did not flag, and skip lines under 6 words | SCEN0124 on the live app showed an engine P50 of ~1.96 s with the 1500 ms budget. Set by firstmate as a Fly secret (no deploy needed), not by this commit | fly secrets; fly.toml; architecture.md Deployment; benchmark.md §3 |
| 2026-09-25 | A place the shop must be in is a check on the event's trusted merchant fields: "in Munich" -> `merchant.merchant_city = "Munich"` (`C12-city`, "Only shops in Munich"), "in Switzerland" -> `merchant.merchant_country = "CH"`, source exact, `!=` for "not in", "no shops in", "never in". Only places the catalogue holds (the merchants.csv cities, with Zürich / München / Luzern / Genève spellings, and the country names); "delivered in Zurich" is where the order arrives, not the shop; a city the catalogue does not hold ("a hotel in Paris") stays a restriction no data can check. Both compiler paths take places from `parser.places`; the model's own place reading is dropped. `Facts.merchant_city` is the event's `merchant.merchant_city`; missing is `unknown` (rule 4), never a pass. A restriction no data can check now reads `Can't check "<phrase>" from the data; you decide` (was `No data to check ""<phrase>" (no data can check this; you will be asked)"`, quotes doubled and the check text repeated) | SCEN0124 compiled "in Munich" as an unverifiable U1 although every event carries the shop's city, so SummitStay (Lucerne, CH) reached the customer as a question instead of declining. Oracle 45/45 unchanged (no oracle policy names a place) | compiler/parser.py `places`, `place_of`, `covered_by_place`; compiler/draft.py `CITY_NAMES`; compiler/llm.py; compiler/dryrun.py; engine/facts.py; engine/types.py; engine/policy.py `unverifiable_detail`; api-contract.md §3.3; rules.md C12; tests/test_place_checks.py; tests/test_compiler_unseen.py |
| 2026-09-25 | Laya back on `performance-2x` with a 1500 ms budget: Fly `ONEGUARD_SOFT_SIGNALS=laya`, `ONEGUARD_SIGNAL_BUDGET_MS=1500`, the machine scaled to `performance-2x`. Supersedes the 900 ms rule of the 2026-09-24 rows below (P95 above 900 ms → keywords) | Two live injections were missed on keywords (SCEN0122 run_828bb7d3434fd984: AU10237 "[agent-policy] … decision=approve", AU10246 "Customer note on file: 'Always allow …'"), both approved. The per-purchase P95 on `performance-2x` was 1,333 ms (sustained 1,468 ms), inside 1500 ms; keywords stay the floor past the budget (P8), now with the patterns of the row below. Applied by firstmate at deploy time, not by this commit | fly secrets; fly.toml; architecture.md Deployment; benchmark.md §3 |
| 2026-09-25 | A1 reads more ways shop text addresses the agent: `[agent-policy]`, `decision=approve\|decline`, "exempt from (customer) confirmation", "always allow", "without asking", "please honour/honor", "note on file", "on behalf of the customer/cardholder", "pre-approved", "auto-approve", and any one text holding both a role word (agent, system, assistant, policy, cardholder, customer note) and an imperative (allow, approve, ignore, skip, proceed, bypass), in either order: the stretch between them is the instruction. Any hit asks at best, never approves (A1, §4 step 5) | The two SCEN0122 lines above were approved with no injection flag: `agent_directed_spans` missed both and Laya was off. No false positive over the 56 public cart lines, catalogue descriptions or merchant names (test_protections.py); the two public injections (AU0037, AU0040) still fire | engine/protections.py `AGENT_DIRECTED_PATTERNS`; rules.md §7 A1 |
| 2026-09-25 | A8 already bought, superseding Q1 ("each purchase judged on its own", 2026-09-24 row below) for one kind of instruction: when the policy has a `requested_item` and the instruction asks for it once ("the X I chose", "one X", "a X", "an X": `Policy.single_item`, read on both compiler paths from the customer's words), the first final approval of that item under the mandate fulfils it; a later purchase of it steps up, `already_fulfilled`, "You already bought the X on {date} for CHF {amount}; approve another?". Only final approvals fulfil (an approval, or a step-up the customer approved); declines and pending, declined or expired step-ups do not, so a re-quote of a declined purchase is not "another" unless something else was bought. Live runs under the same mandate share what was bought; replays keep their own. A8 decides at step 5 only when no other protection asks (a repeat, a split or an injection is named first; the A8 row is in the evidence); a broken rule still declines. `usage.fulfilment` is `{bought, requested: 1}` for these mandates. Oracle: SCEN0004 AU0038, AU0042, AU0044, AU0045 approve → step_up [A8, already_fulfilled]; no other row or matrix message moves (replay-matrix.md) | Live SCEN0122 (Oliver Kunz, CA1022): "the camera lens I chose" was bought 8 times, CHF 5,587.70; `already_fulfilled` was only a label and `usage.fulfilment` always null. Captain decision | rules.md §7 A8, Q1; engine/protections.py `_a8`; engine/decide.py step 5; engine/ledger_base.py `fulfilments`; engine/ledger.py; compiler/draft.py `names_one_item`; acceptance-oracle.yaml; replay-matrix.md |
| 2026-09-25 | A per-unit limit times a stated count of units is the per-order cap: "at most CHF 200 per night" for "3 nights" is also `authorization.billing_amount_chf <= 600` (scope purchase, source inferred, "Total at or below CHF 600 per order (3 nights at CHF 200 each)"); the per-night rule stays. Both compiler paths add it (`parser.stay_cap`), lint takes it as resolved (T2), and it settles the model's order-limit question. Counted items ("two tickets, max CHF 90 each") keep the cart quantity and their question, as the oracle's unseen instruction has it | SCEN0124 compiled with no per-order cap, C2 refused it (`lint_failed` `no_amount_cap`) and the open question was "is the order limit CHF 600 (3 x 200)?". The count of nights is stated, so the cap is arithmetic on the customer's own numbers, not a guess (T2) | compiler/parser.py `stay_cap`; compiler/llm.py (booking example: no question); tests/fixtures/compiler/judging_responses.yaml SCEN0124 |
| 2026-09-25 | Lane requests done by P1 for the three rows above (review asked in the PR): P2 (@tkspahiu-bit) `Policy.single_item`, `Fulfilment`, `LedgerView.fulfilments` (None = not tracked, never a pass), abstract `Ledger.mark_requested_item` in both ledgers, the `requested_item_orders` table (new, so `init_db` adds it without a reset), signal id `A8` with `already_fulfilled` at step 5 after the other asks, a message may end in "?"; P5 (@Dinesh18S) the A1 patterns; P4 (@rozhinaahmadii) `names_one_item`, `stay_cap` and the booking prompt example | One PR so the fix lands before the next deploy (captain) | team-contract.md §3.1; database.md; engine/types.py |
| 2026-09-25 | "Dinner(s)", "supper(s)" and "weeknight(s)" also mean the evening: two C12 rules, `authorization.local_hour >= 17` and `authorization.local_hour < 23` (Europe/Zurich; 17:00 inclusive, 23:00 exclusive), source inferred, texts "Only in the evening: from 17:00 (Swiss time)" / "Only in the evening: before 23:00 (Swiss time)", on both compiler paths (`parser.evening_window`; the LLM path drops any hour the customer did not write in digits and takes the parser's window). An hour the customer states wins on its side ("dinner after 19:00" keeps 19, adds before 23:00). Lunch and breakfast get no hours (acceptance-oracle: "no lunchtime hours invented"). Words after "no" / "never" name no time. The window is no amount cap and no stated number for lint. A broken hour rule reads "Placed at 01:15 Swiss time; you allowed from 17:00." | Live run: SCEN0113 "Weeknight dinners only: one delivery a day, CHF 40 maximum …" compiled without an hour rule, so a Tue 01:15 and a Wed 15:35 (Zurich) delivery were approved | compiler/parser.py `evening_window`, compiler/draft.py `EVENING_HOURS`, compiler/llm.py, engine/policy.py `_fail_text`; SCEN0113 fixture re-recorded |
| 2026-09-25 | one a day = rolling 24 h; calendar-day reading deferred | "One delivery a day" is `cart.purchases_in_period` with `period_days` 1, a rolling 24-hour window on simulated time, so a counterfactual reads "Would approve from tomorrow at 01:15"; reading "a day" as the Zurich calendar day is left for later (captain) | engine/ledger_base.py, compiler/parser.py `_count_per_period` |
| 2026-09-25 | "Same price as last time" at each shop: an order-total rule may hold the value `"last_price_at_shop"` (Viseca's rule format allows a string value, and it round-trips through the mandate snapshot, so no extra rule field). The engine compares each purchase with `LedgerView.last_price_chf_by_merchant[merchant_id]`, the customer's last approved total at that shop: history's `last_price`, replaced by this run's latest final approval there (the run is later than the history it is scored against). Declines and pending step-ups set no price; a changed price the customer approves on a step-up becomes the price. No earlier payment at the shop is `unknown` (rule 4). SCEN0136's "If a price changes, ask me" compiles to it with `on_fail: ask` (id `C1-same`) on both paths, no longer a restriction no data can check. "Same price as last time" keeps one shop's literal price when the purchases it points at were made at one shop (the gym), and becomes the reference when they were made at several. The reference is not a per-order cap (supersedes "Exact price counts as a cap" for it only): a first payment at a new shop has no price to hold it to, so lint still asks for a per-payment limit. The LLM path looks up "last time" by the requested item or the whole instruction, as the parser does (it used the model's price phrase). On the LLM path a per-item amount stays per item only where the parser reads that amount per item ("CHF 200 per night", "CHF 90 each" after counted items); otherwise it is the per-order cap, never the reverse: after the merge with #87, 4 of 6 live SCEN0101 readings made "for CHF 20 or less" a per-item limit (1 of 4 on #87's prompt), which lint rejected, so the parser's reading shipped as `fallback`. A model's unverifiable "no insurance" (an item type the parser reads) becomes that excluded type: the new prompt made 2 of 7 live SCEN0124 readings write it, which would have asked on every booking | Lane request from #48 ("Requests for other lanes", Rozhina, addressed to P2 @tkspahiu-bit), done by P1 under the captain's standing rule. With several subscriptions `value_from last_price` took the latest matching row's shop and held every subscription to that one price. The served history holds no row for the ten judging customers, so in SCEN0136 each shop's first payment asks and the customer's answer sets its price | engine/policy.py `LAST_PRICE_AT_SHOP`, `evaluate_last_price_rule`, `add_ledger_results`; engine/types.py `LedgerView.last_price_chf_by_merchant`; engine/ledger_base.py `last_prices`; engine/ledger.py; compiler/draft.py `last_price_at_shop`, `is_amount`; compiler/resolve.py `at_several_shops`; compiler/parser.py `_same_price`, `_price_change`, `excluded_item_categories`; compiler/llm.py; compiler/lint.py; compiler/dryrun.py; rules.md C1; api-contract.md §3.3; team-contract.md §3.1; tests/test_last_price_per_shop.py |
| 2026-09-25 | "No alcohol" is a checked rule, `items[].contains_alcohol = "false"` (id `C4-alcohol`), on both compiler paths, no longer a restriction no data can check; supersedes that part of the 2026-09-24 row on #48 below. The fact is per cart line: known false when the trusted category cannot be a drink (household, electronics, gift_card, …); otherwise an allowlisted lexicon (English, German, French, Italian) reads the catalogue's `item_name` + `item_description` for the `item_id` and the shop's `item_name` + `item_details`. The catalogue naming alcohol wins (a shop cannot rename IT0168 "Wine and spirits" into "drinks" or call it alcohol-free). Non-drink uses are dropped first ("wine vinegar", "ginger beer", "Weintrauben"). Unknown stays unknown (rule 4): a bare drinks word ("drinks", "Getränke", "aperitif") with no "soft drinks" / "alcohol-free" beside it, or alcohol and alcohol-free wording together ("non-alcoholic beer"). Texts naming neither alcohol nor drinks are known false, as `cart.recurring` is known false without billing language. Tier 2 never reads it. A model reading that still writes "no alcohol" as unverifiable, or lists "alcohol" as a category, ships the alcohol rule | Lane request from #48 ("Requests for other lanes", Rozhina, addressed to P2 @tkspahiu-bit), done by P1 under the captain's standing rule. SCEN0117 ("No alcohol, no gift cards, no cosmetics") asked on every purchase, groceries included, because no field held alcohol. Served catalogue (read-only, 25 Sep): IT0168 "Wine and spirits" (groceries, "Alcoholic beverages sold by a licensed retailer") is the one alcohol item; IT0166 "Drinks and snacks" is "Soft drinks, snacks, and party supplies"; the served history names no alcohol | engine/facts.py `extract_contains_alcohol`; engine/types.py `ItemFacts.contains_alcohol`, `HistoryIndex.catalogue_item_text`; store/history.py; engine/policy.py; engine/decide.py; compiler/parser.py `NO_ALCOHOL`, `_blocked`; compiler/llm.py SYSTEM, `_split_exclusions`; compiler/draft.py; rules.md C4; api-contract.md §3.3; tests/test_alcohol.py |
| 2026-09-25 | Live runs are decided by the stored confirmed policy after a restart. Before #80, `VisecaWorker.bind_policy` (Viseca mandate id → our confirmed policy) lived in memory only, so after any restart or redeploy every live run was decided from the platform's copy of the mandate's rules (`policy_from_snapshot` on the event's `mandate.hard_rules`). That copy carries only field / operator / value, so the requested item (C5), "nothing extra" (C10) and `on_fail: ask` were lost, and a different product or an add-on could be approved. Now, before binding a run whose mandate has no policy in memory, the worker loads the newest mandate stored for that Viseca id (`mandates.viseca_mandate_id`, newest `confirmed_at`) and binds it; a revoked one binds as revoked. The platform's copy decides only when no confirmed policy is stored, and then its checks get the compiler's wording and the decision's `policy_applied.source` is `platform` | Missing is never a pass (CLAUDE.md rule 4): the customer's confirmed restrictions must hold whatever the process lifetime. 8 of the 22 known instructions depend on C5, C10 or `on_fail: ask` (SCEN0104, 0122, 0130, 0136; public 0002, 0004; tickets; gym). Any live run started after a restart and before this fix was checked without those restrictions | viseca/worker.py `_restore_policy`, `_rule_text`; compiler/draft.py `describe_rule`; pipeline.py `policy_applied`; api-contract.md §2 `Decision.policy_applied`; tests/test_worker_restore.py, test_policy_applied.py |
| 2026-09-25 | Startup gap closed. (1) The worker polls as soon as the app is up; with `ONEGUARD_SOFT_SIGNALS=laya` the model loads in a background thread meanwhile and the engine answers the soft signal with keywords until it has (`LayaSignals` switches when its model is set); a failed load keeps keywords and is logged. `/healthz` `signals.backend` is the detector answering now (`keywords` while loading), with `configured`, `model_loading` and `model_loaded` beside it. (2) `make demo-live` and `make demo-offline` drive the server at `ONEGUARD_API_URL` (default `https://oneguard.fly.dev`; was `ONEGUARD_API`) and exit 1 starting nothing when no OneGuard answers `/healthz` there: the local-worker fallback is removed. (3) `fly.toml` deploys with strategy `immediate` | Run live-run_9fd431063e58d38d (00:11): the deploy restarted the only machine at 22:11:03Z, Laya took about 35 s to load before the worker started, nobody polled 22:11:09-22:11:44Z and AU10001 timed out at the platform. The run was started from outside the cloud app, matching the demo-live fallback (it starts a run itself when the server does not answer) | api/app.py lifespan, `_load_signals_model`; api/services.py `active_signals`; engine/signals.py; viseca/demo.py; Makefile; fly.toml; architecture.md; api-contract.md §1; demo-script.md |
| 2026-09-24 | The cloud runs keyword soft signals again: Fly secret `ONEGUARD_SOFT_SIGNALS=keywords` over the Laya image (9a1a68a); the machine is `shared-cpu-1x` with 1 GB (fly.toml), enough for keywords; `ONEGUARD_SIGNAL_BUDGET_MS` unset. Laya is shown on the laptop (demo-script.md step 4) | Captain rule, per purchase (the budget is one wait over every item line): P95 ≤ 450 ms keeps 500 ms, ≤ 900 ms sets 1000, above that keywords. `performance-2x`: per purchase P50 683 ms, P95 1,333 ms (sustained 1,000+ calls: P95 1,468 ms, no throttling), per item line P95 861 ms; every purchase went over 500 ms. `shared-cpu-4x` throttled past its burst balance (per purchase P95 3,712 ms). Keywords answer every pack purchase the same (0 model-only triggers) | fly.toml; architecture.md Deployment; benchmark.md §3; demo-script.md |
| 2026-09-25 | One active mandate per team at Viseca: confirming any policy (any card) supersedes the platform's previous active mandate, so older TMs read `superseded` (or `revoked`) there. C5 treats a DELETE answered 404 or 409 as done: our row is revoked, the platform's status is read (`GET /v1/mandates/{TM}`) and logged, and the customer gets the normal `204`. Any other platform failure (5xx, network, timeout) keeps the existing behaviour: our row stays revoked, C5 answers 503 `upstream_unavailable` with "Try again", nothing is retried server-side. Confirming a policy on stage therefore supersedes the platform's active mandate; irrelevant unless a judging run is open, so no judging run during the stage demo | Read-only `GET /v1/mandates` during the sandbox cleanup: exactly one active mandate per team. Revoking a superseded mandate must not show the customer an error for work the platform already did | api/routes_customer.py `revoke_at_platform`, `_platform_status`; tests/fake_viseca.py confirm, DELETE; demo-script.md pre-show step 5 |
| 2026-09-24 | The worker checks the served `tables.fx_rates` against `facts.FX_TO_CHF` at start: each row's `rate` (number or string) is read as a `Decimal` and compared exactly, no tolerance (`0.95` = `0.950000`); a different rate, a missing or extra currency, a `to_currency` other than CHF, an unreadable rate or a missing table is a mismatch. A mismatch is logged loudly and makes `status().ok` false, so `/healthz` says `degraded` and shows `worker.fx_rates_match` / `fx_rates_mismatch`; the loop keeps polling (`state` stays its own) and the engine keeps `FX_TO_CHF` | The live smoke recorded that `tables.fx_rates` is served but not the rate's JSON type; `FX_TO_CHF` is P2's constant and every CHF amount depends on it, so a silent drift would misprice every limit | viseca/worker.py `fx_rate_mismatches`, `_check_fx_rates`; tests/fake_viseca.py `FakeConfig.fx_rates` |
| 2026-09-24 | Viseca call logging (`viseca_calls`) is off by default: the app and `make demo-live` write rows only when `ONEGUARD_LOG_VISECA_CALLS=true` (debugging only; `fly.toml` leaves it unset). Every reader was checked: nothing in the app reads the table. `/healthz` `worker.last_error` comes from the worker's own state (`VisecaWorker.status()`, plus the start error), and the table was only written, and read by tests. Each call is still logged at DEBUG. Tests that pass `store_sink` explicitly still record | Captain decision: a row per long-poll is a Supabase write every few seconds for a table nothing reads | viseca/client.py `call_sink`; api/app.py `_default_client`; viseca/demo.py; database.md §2; architecture.md Runtime; .env.example; fly.toml; tests/test_api_contract.py |
| 2026-09-24 | Hardening for judge-driven changes (captain intent). (1) The served-pack sync (`seed.sync_served` plus the history-file check) runs at runtime, not only at start, and only in the worker-lease holder: when a bootstrap re-read shows a new `pack_version`; when `/v1/reference-data`, read every 5 min, shows another pack version, other row counts per table or other history-file metadata than the last successful sync; and when an event names a merchant, item, customer or card the store does not know. Concurrent triggers join the sync in flight. The unknown-id sync is bounded at 1 s (less when the 8 s deadline is near), then the purchase is decided with what the store has: no catalogue price range (W6 has no range), merchant category and country from the event's own merchant block, familiarity with no history (C9 unknown or fail, never a pass). The decision gets an `info` evidence row `reference_data` naming the ids. An id a finished sync did not bring starts no further sync (the 5-min check still picks it up if it is served later). A sync failure is logged and never blocks a decision. After a change the history index is replaced in place (`ReloadableHistory`), so the ledger, running pipelines and the API read the new one. (2) Event validation is tolerant of additions: the published schema closes every object (`additionalProperties: false`); the worker validates against a copy with those closures lifted, so required fields, types, enums and formats still hold. Unlisted properties are logged (warning the first time per path) and passed on unread. A missing required field still declines `unevaluable` with an `event_schema` row naming it (`authorization.merchant is missing`). An unknown enum value still declines: a new `authority_status` or `channel` may change what is allowed, and missing is never a pass. Inbound events have no Pydantic models (jsonschema only), so `extra="forbid"` on our API models is unchanged. (3) Every run start re-reads `/v1/bootstrap`: D3 before it creates the run, D8 before it lists the catalogue (`make demo-live` compiles the instruction it lists, so a new pack's instruction is compiled), and the first sight of any other run (`track_run` or its first event, in the background, skipped if read in the last 30 s). A changed human window, decision deadline or long-poll wait is logged and used from then on in every run; the long-poll wait is the configured one capped by `long_poll_max_seconds`, so it also rises back. Also fixed on the way: `test_a_worker_that_loses_the_lease_stands_by_and_polls_again_once_it_is_free` failed about 1 run in 3 (it asserted a poll right after the state flipped to `polling`, before the first poll landed); and the worker defined `_load_cursor`, `_save_cursor`, `_save_state`, `_save_profiles` twice (merge leftover, the later ones won) | Judges may add merchants, items, customers or cards, change instructions or limits, and add event properties while the server runs; before this an extra property auto-declined the purchase (`event_schema`, reproduced with the fake sandbox) and the served pack was read once at start. The data wins (CLAUDE.md); rules 2 and 4 hold: amounts and permissions never come from extras, and an unknown id is never a pass or familiarity. A decision already posted is never re-sent after a sync | viseca/worker.py `_sync_reference`, `_check_reference`, `_reference_rows`, `_load_settings`, `refresh_bootstrap`; viseca/schema.py `check_event`; store/history.py `ReloadableHistory`; api/routes_dev.py D3, D8; architecture.md Runtime; database.md §4; api-contract.md §1.2; tests/test_reference_sync.py, test_demo_profiles.py |
| 2026-09-24 | Laya moves to `performance-2x` (2 dedicated CPUs, 4 GB, lhr); the soft-signal budget is set after measuring there | Captain rule: shared-CPU P95 above 450 ms moves to `performance-2x`, then P95 ≤ 450 ms keeps 500 ms, ≤ 900 ms sets `ONEGUARD_SIGNAL_BUDGET_MS=1000`, above that back to keywords. Measured on `shared-cpu-4x` with the deployed image: a short run (3 × 56 lines) gave P95 344 ms at 1% steal, but the load plus about 340 calls used up the burst balance: per item line P50 320 ms, P95 3,996 ms; per purchase P50 328 ms, P95 3,712 ms, 55 of 135 over 500 ms. The budget is per purchase (one wait over every item line) | fly.toml; benchmark.md §3 |
| 2026-09-24 | A requested product gets its catalogue item type as an inferred C3 rule on both compiler paths, or the open question "Which kind of item or shop counts as <product>?" when the catalogue files it under no single type (compiler/parser.py `PRODUCT_WORDS`, `product_categories`; the LLM path applies the same mapping and asks the same question itself, dropping the model's own copies of it). The mapping names only products every catalogue item they match shares one type with (local `data/items.csv` and the served items, tests/fixtures/compiler/served_items.yaml): "hiking boots" is sporting goods but the served "Winter boots" are clothing, so bare "boots" and "shoes" map to none. A merchant category no item carries (entertainment for "concert tickets") is no item type, so it asks. Consequence on the local pack, compiled rather than hand-built: SCEN0002's reading adds "Only sporting goods", so AU0018 (shoes plus a monthly protection plan) declines (Q10's named alternative) instead of asking, and SCEN0004's adds "Only electronics", so AU0041 also cites item_mismatch (outcomes of AU0043 and the rest unchanged). The oracle and the replay matrix use the hand-built policies and do not move. Served SCEN0104 carts seen so far are all sporting goods; SCEN0122 and SCEN0130 say "nothing extra" | Captain decision (T1): every stated restriction becomes a rule, and a product noun is one; a category is never guessed (T2) | compiler/parser.py `_item`; compiler/llm.py `read_with_llm`, SYSTEM; rules.md §10 |
| 2026-09-24 | Laya runs in the cloud: the image carries the `signals` extra on CPU-only torch with the `laya-typed-decisions` checkpoint baked in (`HF_HUB_OFFLINE=1`), `ONEGUARD_SOFT_SIGNALS=laya` is the image default (no longer a Fly secret), and the machine is `shared-cpu-4x` with 4 GB (`performance-2x` with 4 GB costs more than twice as much). The soft-signal cut-off is `ONEGUARD_SIGNAL_BUDGET_MS` (default 500): kept at 500 if the warm agent_directed P95 per item line on the machine is at most 450 ms, 1000 if at most 900 ms, otherwise the cloud goes back to keywords | Captain decision: the cloud is the only decider, so the model the spike kept must run there. Local container preview (aarch64, CPU torch): P50 285 ms, P95 396 ms per item line; app RSS 2.1 GB with the model | Dockerfile; fly.toml; pipeline.py `signal_budget_s_from_env`; architecture.md Deployment; benchmark.md §3 |
| 2026-09-24 | Laptop run diagnosis and fix: the laptop SCEN0104 (run_91f55e22a2f6caf8) and SCEN0106 (run_db4dadd9d1b2ded8, card CA1576) events that "expired undelivered" were lost because several scenario runs shared the team at once. The platform hands out one request per team at a time. A pending step-up holds that slot until it is answered or its 120 s window ends: `/next` serves it again as `pending_step_up` on every poll and offers nothing else. A request queued behind it still has only 8 s from queueing, so it closes as `timeout` "The request expired before it was delivered". This also hits the cloud (it did, cloud worker alone, 19:50-19:52 UTC). Fixed here (captain decision `laptop-cause`): (a) one worker polls per store: a Postgres advisory lock taken at worker start; any other process stays in `standby` (`/healthz` `worker.state`), does not poll, and takes over when the holder stops; `make demo-live` starts nothing while another worker polls; (b) the `runs` row counters are recomputed from `events_raw` and `decisions`, and `started_at` keeps its first write; (c) a run the platform completed is closed from `GET /v1/scenario-runs/{id}`, on the feed's `scenario.completed` (the feed is now read at least every 5 s) and at start for live rows still `running`. One run at a time (D3, demo-live) lands in p1/demo-profiles. Not modelled yet: the fake sandbox serves queued requests ahead of a waiting step-up, so no test reproduces the single slot | Evidence (sandbox `/v1/authorizations`, `/v1/events`; cloud `viseca_calls`, `decisions`, `events_raw`, `runs`; laptop shell history). 7 runs were started on the team from 18:20 to 19:40 UTC, 6 of them by `make demo-live` (SCEN0104 x2, SCEN0106 x4), overlapping from 19:03. Never delivered: run_91f55e22a2f6caf8 AU10031, 32, 34, 35, 36; run_db4dadd9d1b2ded8 AU10049, 50, 60. Each was queued behind another run's step-up. Example: AU10034-a2f6caf8 was queued at 19:30:06.7 while AU10051-d1b2ded8 was pending (19:30:08-19:32:08); the worker polled 2-3 times a second and got only AU10051. 18 of the 40 undelivered expiries fall entirely inside windows where the laptop polled without a break. After the laptop was killed, the cloud worker alone lost AU10059-7418623e behind a step-up the customer answered in 10 s. Every request that was delivered was posted 0.3-1.7 s after delivery, at least 3.3 s before its deadline. Firstmate's `runs` observations have the same cause: the later SCEN0106 runs (d464261dd12e0fbf, 45c275957418623e, 63242e1cf0476459) are separate runs of the same scenario, with the same source ids AU10049-AU10060 and their own live ids; nothing was re-served. They are `done` because the platform finalised all 12 events, most of them as undelivered timeouts; 2/2/4/5 is what reached the slot. Second defect, cloud too: `runs` counters and `started_at` are the deciding process's in-memory `RunState`, merged over the row by `_save_run`. A restart therefore resets them: run_db4dadd9d1b2ded8 shows delivered 2, started 19:44:35 (the Fly restart), against 9 decisions and a first event at 19:30:07. The two SCEN0104 rows the cloud never saw still say `running` although the platform completed them. Contributing, laptop only: `make demo-live` starts its own `VisecaWorker`. There were two deciders from the first event (Fly plus the demo until the Fly key was unset at 19:02, then `make serve` plus demos), shown by double POSTs, 409 `step_up_resolution_required`, and decisions recorded with signals=off by the demo. `set -a; source .env` exported `ONEGUARD_DATABASE_URL`, so the laptop wrote the Supabase store, not SQLite (the laptop SQLite has no run, decision or call). No laptop process polled 19:05:11-19:08:00 or 19:08:44-19:27:58. macOS logged no sleep and no network change, and the platform only recorded the expiries at the next poll. The 19:33:07 restart ran `make serve` without `.env`, so there was no worker until 19:34:41. Ruled out: `events_cursor` (one shared row; it only feeds reconciliation, not delivery), clock and deadlines, and our 0.5 s step-up pause (the platform offered nothing else) | store/lease.py; viseca/worker.py `_lead`, `_save_run`, `_close_completed`, `_track_unfinished_runs`; viseca/demo.py `run_demo`; database.md §5.1; architecture.md Runtime |
| 2026-09-24 | Messages are "{Outcome} CHF {amount}: {clause}." with one template per field (rules.md §9 E2): the clause is the deciding rule's, at most 15 words, and the counterfactual ("Would approve …") is only ever its own field; supersedes the "Declined CHF <amount>: <short reason>. Would approve <counterfactual>." row below. The amount template keeps the captain's wording, so a per-order decline names the amount twice ("Declined CHF 126.00: CHF 126.00 is over your CHF 120.00 limit."). One clause may be joined: a second deciding warning sign, else "the shop's instructions to the agent were ignored". A lookalike shop (A7) is named whenever it fired (AU0039: "PixelHarbour is 1 letter away from PixelHarbor, a shop you know; it's a different shop."). No authorization id reaches customer text: A3, A4 and A5 name the earlier order by amount and time ("the CHF 70.00 order 6 min earlier", "declined 5 days earlier"), the id stays in `related`; for that, `LedgerView.priors` also carries the run's declines older than 24 h (A5 only reads them by id; A3/A4 keep their windows). Typed rules on `merchant.known_shop` / `merchant.familiar_on_card`, `items[].item_category` (`in`, `not_in`) and `merchant.merchant_category` (`=`, `in`) are checked by the flag checks, so both paths share wording and C9's no-history unknown | Captain decision: the counterfactual is shown on its own line under the message, so the message must not repeat it. Outcomes and reason codes unchanged (oracle 45/45, signals on = off) | engine/policy.py `_fail_text`, `_shared_check`, flag checks, `evaluate_period_rule`, `evaluate_count_rule`; engine/explain.py `_message`, `_counterfactual`; engine/protections.py `_a3`, `_a4`, `_a5`; engine/ledger_base.py, engine/ledger.py `priors`; rules.md §9 |
| 2026-09-24 | Scenario → customer / card bindings come from the platform only, stored per scenario (`scenario_profiles`): the bootstrap `profile`, every run's `fixture_profiles`, every authorization (the worker reads `GET /v1/authorizations` once at start). C12 `live` = bound to a scenario the platform serves now; the local pack's customers keep their `scenario_ids` but are not live once the sandbox stops serving their scenarios. A served scenario never run has no binding yet | The judging sandbox binds a different profile per scenario (SCEN0101 CU1217/CA1331, SCEN0104 CU1415/CA1643, SCEN0106 CU1376/CA1576) and its catalogue and bootstrap name only SCEN0101's; nothing else maps the others before their first run. Supersedes the bootstrap-only C12 binding below | viseca/worker.py `profile_sightings`, `remember_profiles`; api/services.py `bindings`; api/routes_customer.py C12; api/routes_dev.py D3, D8 |
| 2026-09-24 | D3 on a card that is not the one the run's fixture profile uses (a scenario never run, so nobody knew its card) moves the policy to that card: a copy with the same Viseca mandate becomes that card's active policy, the original is marked revoked here only (the Viseca mandate stays in force), and that card's earlier active policy is revoked here and at Viseca as C2 does | The platform assigns the card at run start (mandates carry no card); the customer who signs in must see the policy that decides their purchases | api/queries.py `move_mandate`; api/routes_dev.py `create_run` |
| 2026-09-24 | D3 refuses with 409 `run_active`, naming the run, while the worker follows an unfinished run or the scenario has a run in progress: one the store last saw running (unless the platform's run progress says it is over; an unreadable platform leaves the store's word standing) or one with a purchase still open at the platform (`awaiting_decision`, `pending_step_up`), whoever started it. `force: true` (D3) / `--force` (`make demo-live FORCE=1`) starts anyway. `make demo-live` checks D7 and D8's `active_run_id` first and changes nothing (no draft, no mandate) while a run is in progress | Captain: never start a second run of a scenario over one still in progress. Confirming a new policy on the running scenario's card would also revoke the mandate the run decides under | api/routes_dev.py `active_runs`, `create_run`; viseca/demo.py `run_via_api` |
| 2026-09-24 | `make demo-live` never decides: it starts the run through the server at `ONEGUARD_API` (default the cloud app: C1, C2, D3) and follows D4 / C6 read-only. It falls back to a local worker only when no OneGuard server answers `/healthz` there, and says so | The cloud is the one decider; a second worker next to it polled the same team's requests | viseca/demo.py |
| 2026-09-24 | At start the worker sends the timeout `/resolve` for every step-up the platform still serves as waiting that the ledger already closed by the timeout rule; one the ledger does not know (another decider's) is left alone, one the customer answered is only logged | A timeout `/resolve` lost to a failed call or a restart left the platform serving the step-up for ever; the ledger was already right, so nothing is re-counted | viseca/worker.py `_reconcile_platform` |
| 2026-09-24 | A purchase count per period ("one delivery a day", "one a day", "at most two orders a week", "once a week") compiles to `cart.purchases_in_period` `<=` N ("fewer than N" is `<`), scope period, `period_days` 1 for a day, 7 for a week, 30 for a month, on both paths; supersedes the `unverifiable` reading below. A model's "= N" is read as `<=` N (a gate only caps a count); a count with no window is a question. Lint holds the count and its window to the instruction (T2); a count is never a per-order cap | #51 added the engine field; SCEN0113's "one delivery a day" was a question instead of a check | compiler/parser.py `_count_per_period`; compiler/llm.py prompt, `_convert`; compiler/draft.py `FIELDS`, `count_text`; compiler/lint.py `_check_numbers`; compiler/dryrun.py |
| 2026-09-24 | New field `cart.purchases_in_period` (scope period, `period_days`): purchases on this card in the period window, final approvals plus pending step-ups, compared as count + 1; fail code `period_count_exceeded`, a breach caused only by pending step-ups asks (M5 as for spend). Counted by the same ledger query as period spend (`LedgerView.period_count`), so "one a week" works like "one a day". The worker reconciles Viseca's `approved_spend_in_period_chf` over spend rules only | The judging pack's weeknight meal delivery says "one delivery a day"; no field could express a purchase count | api-contract.md §3.3, §4; rules.md C2; engine/policy.py `evaluate_count_rule`; engine/ledger_base.py `period_counts`; pipeline.py `period_days_of` |
| 2026-09-24 | A decline's message is "Declined CHF <amount>: <short reason>. Would approve <counterfactual>.": the amount once, in the lead; the broken rule in short words ("over your CHF 20.00 per-order limit", "the size is 42", "you haven't bought from this shop before"); the suggestion once, as the last sentence, identical to `counterfactual`. A typed limit's counterfactual reads "Would approve at CHF 20.00 or less". A C10 reason is left out when C5 also failed; a sign that opens with the amount reads "it is …" | Captain decision. The SCEN0101 cloud run read "Declined CHF 38.90: Order total: CHF 38.90; you asked for order total at or below CHF 20.00; would approve with order total at or below CHF 20.00." with the same counterfactual again in its own field. Outcomes and reason codes unchanged | engine/explain.py `_typed_fail`, `_declined_rule`; rules.md E1-E3 |
| 2026-09-24 | A step-up closed by the timeout keeps "Expired: no answer within {n} s; nothing was approved." as its stored and served message ({n} the configured human window), with no counterfactual and `explanation_source` unchanged; the `/resolve` text posted to Viseca is unchanged | Captain decision. The SCEN0101 cloud run kept "Waiting for you CHF 18.00: …" after the platform expired AU10001. The ledger refuses a timeout without the new message, so the live worker, the platform-expired path and the offline expiry all re-render it | engine/explain.py `expired_message`; engine/ledger_base.py, engine/ledger.py `resolve`; viseca/worker.py `expire`; api/offline.py `expire` |
| 2026-09-24 | C9 unknown because the customer has no purchase history yet has reason code `no_purchase_history`; every other unknown keeps its code (`unevaluable` when none fits) | Captain decision: "not everything could be checked in time" misread a first purchase | engine/decide.py `_unknown_code`; api-contract.md §4 |
| 2026-09-24 | The OpenAI default model is `gpt-4.1` (was `gpt-4o-mini`); `ONEGUARD_OPENAI_MODEL` still overrides it | Measured through the real key on the 10 served instructions plus 5 review sentences with the final prompt: `gpt-4.1` passed lint and the floor rule 75/75 (5 runs, 4 at a time) and 30/30 one at a time (p50 3.3 s, slowest 5.6 s, inside the 8 s budget); `gpt-4o-mini` 70/75 (drops the item types of "one meal delivery a day"), `gpt-4.1-mini` 69/75 (drops "weeknight dinners"). A small tier-2 style call takes ~0.8 s on all three | llm/openai_provider.py |
| 2026-09-24 | A purchase count per period ("one delivery a day") compiles to an `unverifiable` rule, not `cart.purchases_in_period` | No field counts purchases yet, and the engine reads every `scope: period` rule as a CHF total: "at most 1 a day" declined a CHF 30 dinner as "over your CHF 1.00 limit" | compiler/parser.py `_count_per_period`; compiler/llm.py prompt |
| 2026-09-24 | "Electronics up to CHF 300 each" (no counted items) is a per-order cap, like "purchases up to CHF 300 each"; "two tickets, max CHF 90 each" stays per item. A bare "and 300 a week" after a CHF amount is CHF. On the LLM path the operator of a stated amount is the customer's boundary word (T3), and an excluded thing no category holds ("no alcohol") becomes an unverifiable rule next to the excluded categories | P1 review of #48 (P4 judging pack): the per-order reading is the tighter one; the rest keep the model from dropping or loosening what the customer wrote | compiler/parser.py; compiler/llm.py `_convert`, `_split_exclusions`; compiler/lint.py `stated_boundary` |
| 2026-09-24 | `ItemFacts` gains `matches_requested: FactValue[bool]` (source `regex` or `model`) and `delivery_date_text: FactValue[date]` (from shop text, source `model`), both unknown by default. C5 and the delivery rule read them only as a second source after the trusted fields (the requested-item name match, the live `delivery_by`); P2 wires `policy.py`, P4 may fill them in tier 2 | P4 request (Rozhina, PR #16): tier 2 had no field to put a requested-item match or a delivery date it read from shop text | engine/types.py `ItemFacts`; api-contract.md §3.3; team-contract.md §3.1 |
| 2026-09-24 | Tier 3 is wired: after the worker posts an engine decision with its template message, it rewrites it in a background task (8 s timeout, off the engine thread, outside the 2 s budget) and stores a changed message with `explanation_source: model` in one short ledger session (`Ledger.set_explanation`); C6 shows it on the next poll. Only while the run has a provider: live runs get one when the models are on (`Services.live_models`: soft signals not `off`, or D5 on), and D5 off clears it for every later decision; a failure or rejected rewrite keeps the template | P4 request (Rozhina, PR #16): `tier3.rewrite_explanation` had no caller | viseca/worker.py `_schedule_rewrite`, `set_models`; api/services.py `set_models`; engine/ledger_base.py, engine/ledger.py `set_explanation`; api-contract.md §3.4; architecture.md |
| 2026-09-24 | "By Friday" stays on the card's simulated date (#32's reading): C2 does not pass the real clock and does not compile again. Rejected: (B) C2 re-resolving the date from its real-clock confirmation time, because real-clock dates would post-date every simulated purchase; (C) C2 re-compiling at confirm with a confirmation time, because a recompile at confirm could differ from what the customer approved | Captain decision (option A) on P4's request from PR #15 | api-contract.md §3.2; api/routes_customer.py `confirm_draft` (unchanged) |
| 2026-09-24 | `compile_instruction(..., confirmed_at=)` exists for later: when given, its Europe/Zurich date is "today" for date phrases; no route passes it, so the compiler uses the card's simulated present (latest history row, M6) | P4 request (Rozhina, PR #15); kept as a parameter only by the captain's option A | compiler/__init__.py, compiler/resolve.py `confirmation_date`; engine/interfaces.py |
| 2026-09-24 | The served reference tables are upserted into the store at worker start (insert missing, update changed, never delete), only when a table's served rows differ (count plus content hash); the history index is reloaded after a change | The judging sandbox serves a superset of `data/` (30 customers vs 20, 41 accounts vs 31, 51 cards vs 41, 78 merchants vs 58, 87 items vs 66; 10 scenarios SCEN0101 to SCEN0136 replace SCEN0000 to SCEN0004); the team profile's card was unknown to the store, so C1 on it was 404. The data wins (CLAUDE.md) | store/seed.py `sync_served`; viseca/worker.py `_sync_served_tables` |
| 2026-09-24 | A served-only scenario is stored with empty `control_question`, `control_theme` and `short_rationale` | The served catalogue has only id, name, instruction and event count; the columns are NOT NULL on Supabase and there are no migrations (database.md §4); they are operator notes nothing decides on | store/seed.py `SERVED_DEFAULTS` |
| 2026-09-24 | D3 checks the scenario against the store's catalogue, not the local pack; a served scenario with no known card runs on any card in the store, one with a known card (the pack's, or the bootstrap profile's) only on that card. C12 binds the bootstrap profile's scenario to its customer and card | The served catalogue names no card; only `/v1/bootstrap` `profile` and a run's `fixture_profiles` do | api/routes_dev.py `_live_scenario`, `profile_bindings`; api/app.py |
| 2026-09-24 | "Under CHF X" reads as "Total under CHF X per order": the cart total in CHF (delivery included), per purchase, strictly below X | P4/P5 review | compiler; api-contract.md §3.3 |
| 2026-09-24 | "Two tickets" reads as `cart.quantity` (total of the requested item across cart lines), as in M13(a) | P4/P5 review | compiler; api-contract.md §3.3 `cart.quantity` |
| 2026-09-24 | "The present I picked" compiles to an `unverifiable` rule, as in M13(d) | P4/P5 review | compiler; api-contract.md §3.3 `unverifiable` |
| 2026-09-24 | "By Friday" is resolved from the card's simulated date (`authorization.timestamp` time), not the real clock; "by" = on or before. Supersedes the date anchor of M13(b) | P4/P5 review | compiler; engine/policy.py `authorization.delivery_by` |
| 2026-09-24 | Soft signals: `ONEGUARD_SOFT_SIGNALS=off` = no soft signal; `laya` = triggered if keywords OR Laya fire, so Laya can only add | P4/P5 review (#14) | api-contract.md §3.7; signals.py |
| 2026-09-24 | `on_fail: ask` covers only the rule compiled from the clause just before "ask me if anything changed", plus the renew same-shop rule (gym's C1-same and C9-same) as the one exception; every other rule keeps its own `on_fail` | P4/P5 review (#15) | compiler; engine/types.py `Rule.on_fail` |
| 2026-09-24 | `dry_run.agent_history` is customer-level and labelled "across your cards"; the dry run itself stays card-scoped and says so | P4/P5 review | api-contract.md §2 `DryRunResult`, §6 item 9 |
| 2026-09-24 | `explanation_source` tag strings: `template` → "Explained by OneGuard"; `model` → "Wording refined by AI · decision made by your rules" | P4/P5 review | api-contract.md §2, §6 items 9-10 |
| 2026-09-24 | Temporary `ONEGUARD_ALLOW_RUNS`: `false` makes D3 answer 409 `runs_disabled` and `make demo-live` exit 2, starting nothing; unset or any other value allows runs. `/healthz` shows `runs_allowed` | Captain: hold the cloud (sandbox in judging mode) until #12 and #24 are on main and redeployed; firstmate removes the secret after that | viseca/client.py `runs_allowed`; api/routes_dev.py `create_run`; viseca/demo.py `main`; api-contract.md §1.2, §3.8 |
| 2026-09-24 | C2 confirms a new mandate that replaces the card's active one: the old one is revoked here and at Viseca (best effort, logged). `open_questions` in the C2 body are ignored like edited check text; the draft's are kept | a card has one policy (the UI keys mandates by card); loosening is a new mandate the customer confirms (CLAUDE.md rule 7) | api/routes_customer.py `confirm_draft` |
| 2026-09-24 | C4 `add_checks` are ids of checks this card's drafts proposed (newest draft first); an id already in force with another rule is 409 `not_pure_addition`, an id never proposed is 422 `validation`, an empty tighten is 409. Viseca gets `PATCH` with all rules | the contract fixes accepted-ids semantics but not where a new check's typed rule comes from; a draft is the only place a typed rule exists | api/routes_customer.py `tighten_policy` |
| 2026-09-24 | C5 revokes the card's latest mandate: locally first (nothing more is approved), then `DELETE` at Viseca; a failed `DELETE` is 503 with the local revoke kept, and a repeat C5 re-sends it (404/409 from Viseca count as done). A card with no mandate is 404 | "no 2xx for work that did not happen" and T6 (revoke stops everything) together | api/routes_customer.py `revoke_policy` |
| 2026-09-24 | C8 and expiry go to whoever owns the step-up: a `replay` run's through the offline runner, any other through the worker (platform first). With no worker connected, C8 on a live step-up is 503 and a lapsed one is closed locally on the next C6 read | Q2 and §3.5 must hold after a reload; a live answer must reach Viseca or not be recorded | api/services.py `resolve`, `close_lapsed` |
| 2026-09-24 | D2 replays under the card's active mandate; without one it compiles the scenario's instruction for that replay only (never stored as a mandate). Replay models are off unless D5 turned them on; D5 switches soft signals and the tier-2 provider for live and replay at once | operators replay before any policy exists (`make demo-offline`); §3.7 | api/routes_dev.py `_replay_policy`, api/services.py `set_models` |
| 2026-09-24 | C1 with a form: rules and a dry-run built in the API (per-order cap fit/violate, known-shop ask against the card's history; categories are not in history); ids `per_order_limit`, `period_limit`, `item_categories`, `known_shop` | the form path has no LLM and P4's dry-run is not reachable through the interfaces | api/policies.py `form_rules`, `form_dry_run` |
| 2026-09-24 | Error code `internal` (500) added to §3.8; `upstream_unavailable` is 503 and also covers the database | every failure needs the envelope | api/errors.py; api-contract.md §3.8 |
| 2026-09-24 | A step-up the platform serves again from `/v1/decision-requests/next` (envelope `status: "pending_step_up"`) is not re-posted; the worker makes sure its expiry is scheduled and pauses 0.5 s before the next poll | Live smoke (SCEN0000): the platform serves a waiting step-up on every poll with no long-poll wait, and a second decision is 409 `step_up_resolution_required`; the worker spun at ~24 polls + 24 re-posts per second for the whole 120 s window | viseca/worker.py `_step_up_still_waiting`; tests/fake_viseca.py |
| 2026-09-24 | The platform expires step-ups itself: at the decision reply's `step_up_expires_at` (accepted time + `limits.step_up_timeout_seconds`) Viseca declines an unanswered step-up with `decision_source: timeout`, reason `step_up_expired`, "The confirmation window expired."; our `deadline_at` is that same moment | Live smoke (SCEN0000): the platform finalized AU0001 26 ms after `step_up_expires_at`, before our timeout `/resolve`, which got 409 `authorization_not_pending` | viseca/worker.py `_await_answer`; tests/fake_viseca.py |
| 2026-09-24 | Our expiry reads the platform's state first: at the deadline the worker reads `GET /v1/authorizations?run_id=<run>` (the platform filters by `run_id` and `status`, not `authorization_id`). Already final there → record that result (declined by timeout → `expired`, `resolved_by: timeout`, reservation released), post nothing. Still pending → `/resolve` `decline` with the Q2 message; on a 409, read again and record what it says. Never a second `/resolve` for the same live id (customer or timeout), across redelivery, retries and a rescheduled expiry | Captain decision after the live smoke and a CI race: a stale `pending_step_up` envelope arriving after the expiry re-sent `/resolve` | viseca/worker.py `expire`, `_platform_result`, `_claim_resolve`; rules.md Q2; api-contract.md §3.5 |
| 2026-09-24 | The worker downloads the history file at start and hashes it | Live `/v1/reference-data` names the file (`history: {path, rows, format}`) but serves no SHA-256; on 24 Sep the served file was byte-identical to `data/metadata.json` (eb562bdc…) | viseca/worker.py `_check_reference_data` |
| 2026-09-24 | Timeouts come from bootstrap `limits.decision_timeout_seconds` (8), `limits.step_up_timeout_seconds` (120), `limits.long_poll_max_seconds` (25) | Live bootstrap shape; the human window had matched only the default before | viseca/worker.py `_load_settings` |
| 2026-09-24 | The sandbox's team reset is disabled (judging mode): `POST /v1/team/reset` with `{"confirmed": true}` → 403 `reset_disabled` "Team reset is disabled during judging"; bootstrap says `features.reset: false`. Team state (mandates, runs, the events cursor) cannot be cleared, so every live run stays on the team's record | Live smoke: without a body → 422 `validation_error` (`details` names `body.confirmed`); the client now sends it | viseca/client.py `reset_team` |
| 2026-09-24 | "M1" Session watch after a burst: the next otherwise-clean purchase asks once (`session_watch`); the customer's approval turns the watch off, a no or a timeout keeps it on. Per card, carries into later live sessions; replays keep their own | P2 bundle v2 (PM decision). rules.md W-rule 4 contradicted api-contract §3.6; the contract's reading wins | rules.md W-rule 4; acceptance-oracle.yaml AU0031, AU0032; api-contract.md §3.6, §4; engine/ledger.py `_frozen`, engine/decide.py |
| 2026-09-24 | "D8" SCEN0001, branch "AU0006 pending (reserved)": AU0009 → `step_up` (C2, M5): 258.50 on final approvals fits, 389.00 only with AU0006 and AU0008 reserved. Knock-on: AU0011 → `step_up` (C2, M5): 158.00 fits, 312.50 only with three orders waiting | P2 bundle v2. rules.md M5: a breach caused only by reservations asks | acceptance-oracle.yaml AU0009, AU0011 |
| 2026-09-24 | "M13(a)" "Two tickets" = total quantity of the requested item across the cart (1 line × 2 or 2 lines × 1), field `cart.quantity` | P2 bundle v2 (PM decision). Per-line `items[].quantity = 2` rejects legitimate 2 × 1 baskets | acceptance-oracle.yaml unseen_instructions; api-contract.md §3.3 `cart.quantity`; engine/policy.py |
| 2026-09-24 | "M13(b)" "Arrive by Friday" = the Friday after the instruction was confirmed, as a literal date; "by" = on or before | P2 bundle v2 (PM decision). A per-purchase Friday would let later purchases arrive later | acceptance-oracle.yaml unseen_instructions |
| 2026-09-24 | "M13(c)" "Same price as last time, ask me if anything changed": a different price asks (`on_fail: ask`), it is not declined; "renew" = the same gym, also `on_fail: ask` | P2 bundle v2 (PM decision). The customer's words say ask | acceptance-oracle.yaml unseen_instructions; engine/types.py `Rule.on_fail` |
| 2026-09-24 | "M13(d)" "The present I picked" / "official ticket seller" are rules with field `unverifiable`: always unknown, ask once, then remember the customer's yes for that shop and item under that mandate | P2 bundle v2 (PM decision). No data can check them (T2); remembering cuts repeat approvals | acceptance-oracle.yaml unseen_instructions; api-contract.md §3.3 `unverifiable`, §2 `MandateUsage.confirmations`; engine/ledger.py `_confirmed_keys`, engine/policy.py `add_ledger_results` |
| 2026-09-24 | "M18" Every run writes `runs.kind` (`live` / `replay`); remembered answers and the session watch carry over only between live runs, and a run with no `runs` row counts as a replay. The worker writes its live row before a run's first decision | P2 bundle v2. Replaying a scenario must always decide the same way | viseca/worker.py `_handle`; database.md `runs` |
| 2026-09-24 | "M20" Reason codes `rule_not_met`, `unusual_activity`, `session_watch` | P2 bundle v2. decide emits them; every code is listed before it is emitted | api-contract.md §4 |
| 2026-09-24 | "M10" `backend/scripts/bench_engine.py` belongs to P2 | P2 bundle v2. The plan asks P2 to write the engine benchmark (P2-3) | team-contract.md §1 |
| 2026-09-24 | No returns (0 days) for: final sale, all sales final, no returns, no refunds, non-returnable, non-refundable (except when the clause is about shipping, delivery or a fee), returns not accepted, cannot be returned, not eligible for return | P2 bundle v2 (product decision) | engine/facts.py `return_window_days`; rules.md C7 |
| 2026-09-24 | Customer rules are checked only when the customer stated them (no known-shop check unless asked) | P2 bundle v2 (product decision) | engine/policy.py `evaluate_rules` |
| 2026-09-24 | Size rules apply to the requested item's lines only; add-ons are judged by "nothing extra" (C10) and the recurring-cost protection (A6) | P2 bundle v2 (product decision) | engine/policy.py; rules.md C6 |
| 2026-09-24 | A restriction we have no data for ("official ticket seller") is unknown, never a pass | P2 bundle v2 (product decision) | engine/policy.py; rules.md T2 |
| 2026-09-24 | Period limits count final approvals plus reserved pending step-ups; a breach caused only by reservations asks (`period_reserved_pending`, M5) | P2 bundle v2 (product decision) | engine/policy.py `evaluate_period_rule`; engine/decide.py |
| 2026-09-24 | engine overrun follows the uncertainty setting (rules D2, D3) | Captain decision. An engine still unfinished `POST_MARGIN_S` before `deadline_at` follows `policy.uncertainty_policy` of the policy the worker decides with (the bound policy, else the event's mandate snapshot): `ask` → `step_up`, `decline` → `decline`, `approve` → `step_up`, all with `unevaluable`. Approve is never automatic (D2). The step-up is exactly the one of the row below (message, reservation, C8, timeout expiry). The decline says "Declined: we could not finish checking this purchase in time, so nothing was approved.", is final, reserves and counts nothing, and schedules no expiry. A missing or unknown setting is read as `ask` (logged). Either way the late engine result records the claimed decision (first claim wins) and posts nothing. Engine errors and schema-invalid events still decline | viseca/worker.py `_overrun_entry`, `_post_overrun`, `overrun_setting`, `OverrunClaims` |
| 2026-09-24 | Superseded by "engine overrun follows the uncertainty setting" above. engine overrun → step_up (rules D3, P6) | Captain decision. D3: what is known may be Ask; P6: a slow check must not block ordinary shopping, and nothing is approved without the customer. An engine still unfinished `POST_MARGIN_S` before `deadline_at` posts `step_up`, `unevaluable`, "We couldn't check this purchase in time; please review it", then waits on the customer like any step-up: pending ledger entry with its reservation, C8, expiry at accepted time + human window with the timeout decline. The late engine result records that step-up (first claim wins) and posts nothing. Engine errors and schema-invalid events still decline. Supersedes the overrun part of the "Engine over budget …" row below | viseca/worker.py `_overrun_step_up`, `OverrunClaims` |
| 2026-09-24 | Half EU sizes (43.5, 43,5, 43½, 43 1/2) and letter sizes (XS–XXXL, small/medium/large) are read; size ranges ("sizes S-XL") are not | P2 product decision (facts + policy PR) | engine/facts.py; api-contract.md §3.3 `items[].size_eu`, `items[].size_letter` |
| 2026-09-24 | "Non-refundable" means no returns, except when the clause is about shipping, delivery or a fee; "exchange / store credit only" is unknown | P2 product decision (facts + policy PR) | engine/facts.py `return_window_days`; rules.md C7 |
| 2026-09-24 | Recurring is billing language only; bare "weekly"/"monthly" in product names (8 grocery lines say "Weekly") is ignored | P2 product decision (facts + policy PR) | engine/facts.py `recurring`; rules.md A6 |
| 2026-09-24 | Shop-text extraction is English only for now | P2 product decision (facts + policy PR) | engine/facts.py |
| 2026-09-24 | Contradictory shop terms (two different values) are unknown and flagged `contradictory`, reason code `shop_terms_contradictory` | P2 product decision (facts + policy PR) | engine/facts.py; api-contract.md §4 |
| 2026-09-24 | Add-on lines without terms do not make the order's return window unknown; the order uses the strictest stated line | P2 product decision (facts + policy PR) | engine/facts.py; rules.md C7 |
| 2026-09-24 | Timeout message formats the `/v1/bootstrap` human window: "No answer within {n} s; nothing was approved"; identical to the documented text at the default 120 s | the message names the real number (E2) if the platform window ever differs | viseca/worker.py `timeout_message`; rules.md Q2 |
| 2026-09-24 | Viseca response shapes not fixed by the docs are read tolerantly: timeouts from the first bootstrap key naming human+window (seconds, `_ms` converted), history metadata = the reference-data object with `sha256` naming the history file, run progress `status`/`total`, step-up accepted time from `accepted_at`/`decided_at`/… else our receipt time, feed items `authorization_id` + `status` | no live key during P1-1; one place to adjust once the real payloads are seen | viseca/worker.py |
| 2026-09-24 | Reconciliation: `context.approved_spend_in_period_chf` vs our final approvals in the run over the policy's shortest period (whole run without one); feed mismatch only when Viseca reports a final status our ledger does not have. Both are `ledger_mismatch` info evidence (feed rows on the next decision) and never change an outcome | the platform's period is unspecified; transient pending states are not mismatches | viseca/worker.py; api-contract.md §3.4 |
| 2026-09-24 | Overrun part superseded by "engine overrun follows the uncertainty setting" above. Engine over budget and still unfinished `POST_MARGIN_S` before `deadline_at`, engine error, or an event failing the schema → `decline` with `unevaluable` before the deadline | D3 plus P3: what could not be checked is never approved; a step-up would ask the customer to approve an unchecked purchase | viseca/worker.py |
| 2026-09-24 | Our own mandate not active (revoked) → pipeline declines with `card_or_authority_inactive` (step 1) before rules run, stubs included | T6/Q6 must hold before P2's decide lands | pipeline.py |
| 2026-09-24 | Redelivery re-posts the stored decision; a 409 means Viseca has it. A 400/422 on the full decision body is retried once without `evidence`/`engine_version` | a decision must land before the deadline even if the platform rejects our evidence format | viseca/worker.py |
| 2026-09-24 | Expiry part superseded by "Our expiry reads the platform's state first" above. Customer answer: POST `/resolve` first, then the ledger (a failed POST changes nothing). Expiry: ledger first, then POST (the window is closed locally whatever Viseca says). Both serialised by one lock. `/resolve` evidence carries `{rule: resolved_by, outcome: info, detail: customer\|timeout}` | exactly one of them closes a step-up; nothing is approved after the window | viseca/worker.py |
| 2026-09-24 | A mandate with no confirmed policy bound in the worker is decided from the event's mandate snapshot (`hard_rules` as typed rules, convenience fields restated where the field vocabulary allows) | the snapshot is what the customer confirmed at Viseca | viseca/worker.py `policy_from_snapshot` |
| 2026-09-24 | team docs reconciled with core docs; database stack, timeout resolve, models path fixed | conflict list from the team-docs commit | CLAUDE.md / AGENTS.md rule 6, Stack; architecture.md Dependencies, Deployment; api-contract.md header, §3.1, §3.4, §3.5, §4, §6; database.md §1; team-contract.md §3.1; team-plan.md P1-0, P5-2; README.md Quick start |
| 2026-09-24 | Use every provided field and endpoint: step 1 names `authority_status` and `card_status_at_attempt`; C7 covers `order_cancellable`; `merchant_mcc` secondary evidence for C8; `merchants.recurring_capable` evidence for A6; W6 price outside catalogue range (weak); `HistoryIndex.agent_history` / `item_price_range`; `DryRunResult.agent_history`; reconciliation via `GET /v1/events` as well as `context`; startup pack check against `/v1/reference-data` | coverage audit of the data pack and API | rules.md §4 step 1, C7, C8, A6, W6; api-contract.md §2, §3.4; database.md §3; team-plan.md P1-1, P3-1, P5-2 |
| 2026-09-24 | Frontend contract merged into docs/api-contract.md; frontend/API-CONTRACT.md is a pointer | One canonical contract; the backend-facing requirements (envelopes, consistency matrix, check wording, checklist) now live in it | api-contract.md §1.0, §3.1a, §3.9, Appendix A; frontend/API-CONTRACT.md |
| 2026-09-24 | Spend meter reads `Mandate.usage` first, check wording second | `usage` is the engine ledger's own view; parsing limits back out of prose fails silently (the fixture's "across any rolling 7 days" never matched) | frontend/src/lib/spend.ts `limitsFromMandate`; api-contract.md §3.9 |
| 2026-09-24 | `Decision.explanation_source` ('template' \| 'model'); C6 may return the template message first and the model rewrite on a later poll | Viseca Q&A | api-contract.md §2, §3.4, §6 |
| 2026-09-24 | C8 no longer bound to a hash of the step-up rendering; the step-up still renders the complete purchase | Viseca Q&A | api-contract.md §3.5 |
| 2026-09-24 | Expired step-up: backend posts `/resolve` `decline` with the timeout message; `Decision.resolved_by: 'customer' \| 'timeout'` (NEW) | Viseca Q&A | api-contract.md §3.5 |
| 2026-09-24 | Tier 2 extraction and Tier 3 explanation use the compiler's provider interface (OpenAI first, model-agnostic); soft signals unchanged | Viseca Q&A | api-contract.md §3.7 |
| 2026-09-24 | P8: with every model off, each decision is identical or more cautious; models never change a decision the deterministic rules have made | Viseca Q&A | rules.md P8 |
| 2026-09-24 | Three tiers: 1 deterministic rules; 2 constrained LLM fact extraction only when a rule is `unknown` (schema-validated, 1.5 s, `source: model`); 3 LLM explanation rewrite after posting, template first | Viseca Q&A | rules.md §4a |
| 2026-09-24 | M5: a purchase that fails C2 only because of reservations is Ask, and the message names the waiting order | Viseca Q&A | rules.md M5 |
| 2026-09-24 | Q2 closed: expiry posts `/resolve` `decline` ("No answer within 120 s; nothing was approved", `resolved_by: timeout`); not spent | Viseca Q&A | rules.md Q2 |
| 2026-09-24 | Q6 closed: decline anything received after revoke; UI shows cancelled once the platform confirms | Viseca Q&A | rules.md Q6 |
| 2026-09-24 | Q7 closed: known shop is customer-level; Viseca handles it | Viseca Q&A | rules.md Q7 |
| 2026-09-24 | Q8 closed: superseded by the new M5 | Viseca Q&A | rules.md Q8 |
| 2026-09-24 | Q11: hidden scenarios exist at judging; C12 in scope, tested with invented instructions | Viseca Q&A | rules.md Q11 |
| 2026-09-24 | E8: after posting, an LLM may rewrite the explanation from structured evidence only; it never sees or alters the decision | Viseca Q&A | rules.md E8 |
| 2026-09-24 | AU0008 with AU0006 pending (reserved) → step_up [C2, M5] | Viseca Q&A | acceptance-oracle.yaml AU0008 |
| 2026-09-24 | Four invented instructions with expected typed rules; a phrase with no field stays `unknown` and becomes an open question | Viseca Q&A | acceptance-oracle.yaml `unseen_instructions` |
| 2026-09-24 | New typed-rule fields for C12: `items[].unit_price_chf`, `items[].quantity`, `merchant.merchant_country`, `authorization.delivery_by`, `authorization.weekday`, `authorization.local_hour` (Europe/Zurich) | Viseca Q&A | api-contract.md §3.3 |
| 2026-09-24 | `oneguard/llm/` provider interface (OpenAI first, Anthropic stub) shared by compiler, tier-2 extraction and tier-3 explanation | Viseca Q&A | architecture.md |
| 2026-09-24 | Dependencies: `openai` added, `anthropic` kept optional | Viseca Q&A | architecture.md |
| 2026-09-24 | Latency: tier 2 ≤ 1.5 s inside the 2 s budget; tier 3 after posting, outside it | Viseca Q&A | architecture.md |
| 2026-09-24 | CLAUDE.md rule 1: no model decides; models may extract a fact, rewrite the explanation after posting, or answer agent_directed via Laya (mirrored to AGENTS.md) | Viseca Q&A | CLAUDE.md, AGENTS.md |
| 2026-09-24 | `openai` added to the `compiler` extra alongside `anthropic` | Viseca Q&A | backend/pyproject.toml |
| 2026-09-24 | `ONEGUARD_LLM_PROVIDER=openai` and `OPENAI_API_KEY`; `ANTHROPIC_API_KEY` kept as the alternative; `ONEGUARD_COMPILER` removed; VITE_ lines removed (frontend has its own .env.example) | Viseca Q&A | .env.example |
| 2026-09-24 | A6 (hidden recurring cost) is Ask unless C10 is stated | Recurring cost is the risk; a customer may still want the plan; Ask is the useful intervention | rules.md §7, Q10 |
| 2026-09-24 | A1 flags the purchase, not the shop for the run | Otherwise AU0042/AU0045 could never approve at PixelHarbor | rules.md §7 |
| 2026-09-24 | W4 suppressed within a stated per-order limit | Fires on 7/11 SCEN0004 purchases otherwise | rules.md §8 |
| 2026-09-24 | Known shop = customer-level, pending Viseca answer | AU0044; issuer may only see own cards | rules.md Q7 |
| 2026-09-24 | Each purchase judged on its own (no fulfilment counter). **Superseded 2026-09-25 for single-item instructions (A8)** | Viseca notes call AU0023 compliant and AU0042 a legitimate re-quote | rules.md Q1 |
| 2026-09-24 | Laya used for one question: agent_directed (threshold 0.6, 500 ms timeout, loaded once at startup, called from a thread pool). recurring_billing and item_kind dropped. | Spike: injections 0.71/0.83, no false positive above 0.38, P95 175 ms for three questions, 48 ms for agent_directed alone (P50 28 ms); recurring 0.46 miss on the only example; 13/13 monitors misclassified | signals.py |
| 2026-09-24 | Exact price counts as a cap: an amount rule with `<`, `<=` or `=` is a per-order cap, `>=` and `>` are not. C2 re-lints through the registered `lint_accepted` (P4's compiler) instead of the API's own `relint`, which is removed; the form dry-run stays in the API | The gym renewal's "same price as last time" is `= CHF 59` and bounds one purchase; a floor bounds nothing. One lint for C1 and C2 | api/routes_customer.py `confirm_draft`, api/policies.py `PER_ORDER_OPERATORS`, compiler/lint.py, engine/stubs.py |
| 2026-09-24 | C9 known shop with no purchase history yet (no approved purchase in history on any card, none in this run) is `unknown`, not a fail: the uncertainty setting decides. The customer's yes on a known-shop ask is remembered for the shop, whatever the items (`rule|merchant|*`); `unverifiable` answers stay per shop and item | The judging pack's profile CU1217 has no history rows, so "a shop I use regularly" could never pass and every purchase would decline. Oracle unaffected: every public customer has history | rules.md C9; engine/policy.py `add_ledger_results`; engine/ledger_base.py `confirmation_keys` |
| 2026-09-24 | `instruction` is the customer's words verbatim (text path) or exactly "Built from the form" (form path), never the joined check texts; Viseca still gets the form's accepted checks as the mandate instruction. Form drafts and their mandates stored with joined texts are set to "Built from the form" at start (`queries.restore_form_instructions`: draft `compiler = 'form'`, mandate matched by card and `confirmed_at`), no schema change | CardDetail's "Your words" showed "Total at or below CHF 20 per order. …" for form policies | api-contract.md §2, §3.2; api/routes_customer.py `create_draft`, `confirm_draft`; api/app.py lifespan |
| 2026-09-25 | Tier 3 keeps names and numbers: the model gets `[SHOP]`/`[ITEM]` placeholders, which are turned back into the names, and a rewrite is used only if it contains every number (verbatim) and every shop and item name (case-insensitive) of the template message; otherwise the template stays with `explanation_source: template` | SCEN0117: rewrites said "the item is gift card" and "bought from the shop before" where the template named them | engine/tier3.py; rules.md E8; api-contract.md §3.4 |
| 2026-09-25 | `ledger_mismatch` evidence is operator-only: stored and posted to Viseca, sent by C6 only with `?operator=1` | "Viseca counts CHF 157.04 …; our ledger counts CHF 217.65" is reconciliation for the operator, meaningless to the customer | api/routes_customer.py `list_decisions`; api/models.py `OPERATOR_ONLY_EVIDENCE`; api-contract.md §1.1, §3.4 |
| 2026-09-25 | A C8 answer re-renders the step-up's stored and served message: "Approved by you CHF {amount}: {clause}" / "Declined by you CHF {amount}: {clause}", the asked clause verbatim, counterfactual unchanged; the `/resolve` message posted to Viseca is unchanged | "Waiting for you" stayed on answered purchases; mirrors the timeout re-render (Q2) | engine/ledger_base.py `answered_message`; rules.md Q2; api-contract.md §3.5 |
| 2026-09-25 | `Mandate.usage` counts the card's newest run, whichever mandate decided it; a D3 move rebinds a run in flight to the moved mandate (`Worker.move_policy`); D6 compares the run's mandate by policy lineage (same Viseca mandate). Home's `OverviewHero` counts each card's newest run like Activity's chips, not the last 7 simulated days, and Home's policy rows show `usage` | SCEN0117 on the cloud: the policy moved to CA1559 as a new id after the run had bound the original, so C3 usage read 0 for the whole run; the 13-day run's 3 approvals fell outside Home's 7-day window while Activity listed them | api/routes_customer.py `_with_usage`, api/queries.py, viseca/worker.py, frontend Home/OverviewHero, api-contract.md §2, §6 item 18 |
